# Local Research Capture

Local-first research capture keeps raw source references close to generated notes. It is useful for agent workflows because every summary can point back to the file or URL that produced it.

The MVP should avoid hidden network access. URL fetching must be explicit, fixture-driven in tests, and visible in generated briefs.

Exporting an oss-ideas qualification format lets researchers move from evidence to a decision without rewriting source metadata.
